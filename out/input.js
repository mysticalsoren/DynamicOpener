function modifier() {
    /* 
    Can be run here as well, though, will only run on do / say / story.
    
    const data = DynamicOpener.initialize()
    
    // if (data) {
    //     data[""] = ""
    //     data[""] = data[""].toLowerCase() === "" ? "true" : "false"
    // }
    
    DynamicOpener.apply()
    */
    // subsequent libraries

    return { text: text, stop: false }
}
modifier();